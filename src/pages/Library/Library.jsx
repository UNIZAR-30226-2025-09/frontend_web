import React from "react";
import "./Library.css"; // Asegúrate de que el CSS existe

const Library = () => {
    return (
        <div className="library-content">
            <h1>Tu Biblioteca</h1>
            <p>Aquí encontrarás tus playlists y canciones guardadas.</p>
        </div>
    );
};

export default Library; // 📌 Asegúrate de que esté el `export default`
